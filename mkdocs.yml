# 1. Replaces url:
site_url: https://spiro9-peef.github.io/s9-storywiki-pages/

# 2. Replaces branding.title:
site_name: Ultraverse Exploratory Center

# 3. Replaces input: . and output: public
# MkDocs automatically looks for the /docs/ folder and builds to standard site paths
docs_dir: docs

theme:
  name: material
  
  palette:
    - scheme: slate
      primary: custom
      accent: custom
    
  features:
    - navigation.tabs # Optional: Creates a top bar to switch sub-wikis like Google Sites
    - navigation.indexes # Makes clicking a folder automatically load its index/Home file
    
plugins:
  - search
  - awesome-pages:
        filename: nav.yml
    
hooks:
  - hooks.py
  
extra:
  timeline_years:
    rotc: dynamic    # Automatically matches whatever year it is in real life
    tsr: 2155

extra_css:
  - stylesheets/sitestyles.css

extra_javascript:
  - javascripts/theme-engine.js